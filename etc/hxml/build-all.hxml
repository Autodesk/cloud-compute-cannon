#Builds ALL haxe projects, apps, client libraries, etc

-cmd echo "Building fluent configs from templates"

--next

etc/hxml/build-fluent-config.hxml

--next

-cmd echo "Building all server tests"

--next

test/testsCompile.hxml

-cmd echo "Building server"

--next

etc/hxml/server-build.hxml

-cmd echo "Building CLI tool"

--next

etc/hxml/cli-build.hxml

-cmd echo "Building Server API tests"

--next

-cmd echo "Building Development CI server"

--next

etc/hxml/dev-ci-server-build.hxml