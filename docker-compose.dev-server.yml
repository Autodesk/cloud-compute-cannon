compute:
  command: ["nodemon", "-w", "build/", "-w", "/config/ccc.yml", "build/cloud-compute-cannon-server.js"]
  volumes:
    - ./:/config
    - ./build:/app/build
  environment:
    LOG_LEVEL: "${LOG_LEVEL}"

redis:
  log_driver: "json-file"
  log_opt: {}

elasticsearch:
  log_driver: "json-file"
  log_opt: {}

fluentd:
  log_driver: "json-file"
  log_opt: {}

registry:
  log_driver: "none"
  log_opt: {}