#!/usr/bin/env sh
docker-compose rm -fv && docker-compose build redis registry compute && DOCKER=true AWS=false VAGRANT=false docker-compose -f docker-compose.yml -f docker-compose.override.yml -f docker-compose.test.yml up --abort-on-container-exit redis registry compute
