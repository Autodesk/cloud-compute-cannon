#!/usr/bin/env sh
docker-compose rm -fv redis registry fluentd elasticsearch kibana && docker-compose build redis registry fluentd elasticsearch kibana && docker-compose up redis registry fluentd elasticsearch kibana